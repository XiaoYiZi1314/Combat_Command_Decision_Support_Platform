<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccds.vehicle.vehicle.mapper.VehicleMapper">

    <resultMap id="BaseResultMap" type="com.ccds.vehicle.vehicle.entity.VehicleDO">
        <id column="id" property="id"/>
        <result column="station_id" property="stationId"/>
        <result column="type" property="type"/>
        <result column="vehicle_type" property="vehicleType"/>
        <result column="plate" property="plate"/>
        <result column="old_plate" property="oldPlate"/>
        <result column="status" property="status"/>
        <result column="model" property="model"/>
        <result column="maker" property="maker"/>
        <result column="water_cap" property="waterCap"/>
        <result column="foam_cap" property="foamCap"/>
        <result column="powder_cap" property="powderCap"/>
        <result column="work_height" property="workHeight"/>
        <result column="engine_no" property="engineNo"/>
        <result column="vin" property="vin"/>
        <result column="color" property="color"/>
        <result column="made_date" property="madeDate"/>
        <result column="equip_date" property="equipDate"/>
        <result column="notes" property="notes"/>
        <result column="gmt_create" property="gmtCreate"/>
        <result column="gmt_modified" property="gmtModified"/>
    </resultMap>

    <sql id="BaseColumns">
        id, station_id, type, vehicle_type, plate, old_plate, status, model, maker,
        water_cap, foam_cap, powder_cap, work_height, engine_no, vin, color,
        made_date, equip_date, notes, gmt_create, gmt_modified
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ccds_vehicle (station_id, type, vehicle_type, plate, old_plate, status,
                                   model, maker, water_cap, foam_cap, powder_cap, work_height,
                                   engine_no, vin, color, made_date, equip_date, notes)
        VALUES (#{stationId}, #{type}, #{vehicleType}, #{plate}, #{oldPlate}, #{status},
                #{model}, #{maker}, #{waterCap}, #{foamCap}, #{powderCap}, #{workHeight},
                #{engineNo}, #{vin}, #{color}, #{madeDate}, #{equipDate}, #{notes})
    </insert>

    <update id="updateById">
        UPDATE ccds_vehicle
        SET station_id = #{stationId},
            type = #{type},
            vehicle_type = #{vehicleType},
            plate = #{plate},
            old_plate = #{oldPlate},
            status = #{status},
            model = #{model},
            maker = #{maker},
            water_cap = #{waterCap},
            foam_cap = #{foamCap},
            powder_cap = #{powderCap},
            work_height = #{workHeight},
            engine_no = #{engineNo},
            vin = #{vin},
            color = #{color},
            made_date = #{madeDate},
            equip_date = #{equipDate},
            notes = #{notes},
            gmt_modified = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM ccds_vehicle WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM ccds_vehicle
        WHERE id = #{id}
    </select>

    <select id="selectByStation" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM ccds_vehicle
        WHERE station_id = #{stationId}
        ORDER BY id ASC
    </select>

    <select id="selectByStations" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM ccds_vehicle
        WHERE station_id IN
        <foreach collection="stationIds" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
        ORDER BY station_id ASC, id ASC
    </select>

    <select id="selectByStationAndPlate" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/>
        FROM ccds_vehicle
        WHERE station_id = #{stationId}
          AND plate = #{plate}
          <if test="excludeId != null">
              AND id != #{excludeId}
          </if>
    </select>

</mapper>
